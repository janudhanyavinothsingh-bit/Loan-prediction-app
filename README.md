# Loan Prediction App

A machine learning project that predicts loan approval using applicant details and financial parameters.  
Built with **Python, scikit-learn, and Streamlit**.

---

## Project Structure
- `app.py` → Streamlit app for interactive loan prediction
- `loan_prediction_model.pkl` → Trained ML model
- `requirements.txt` → Dependencies
- `loan_approval_dataset_ml.ipynb` → Jupyter Notebook (data exploration, training, evaluation)

---

## Features
- Sidebar inputs for applicant details
- Rule-based + ML prediction
- Approval probability display
- Clear rejection reasons (e.g., low CIBIL score)
- Prediction history with option to clear
- Explainable predictions

---

## Installation
Clone the repo:
```bash
git clone https://github.com/janudhanyavinothsingh-bit/loan-prediction-app.git
cd loan-prediction-app
nstall dependencies:

bash
pip install -r requirements.txt
Run the app:

bash
streamlit run app.py
Dataset
The dataset includes applicant demographics, income, loan details, and asset values.
Used for training a Random Forest model to predict loan approval.

Deployment
You can deploy this app on Streamlit Cloud:

Go to Streamlit Cloud.

Connect your GitHub repo.

Deploy app.py with requirements.txt.

Share the public link.

Author
Janu Dhanya Vinoth Singh  
Learning ML deployment with Streamlit and GitHub.

Code

---

---

Once you add this file and push it (`git add README.md && git commit -m "Add README" && git push`), your repo front page will look professional and ready to impress your tutor.

Do you want me to also show you how to **deploy this repo on Streamlit Cloud** so your tutor can run the app online without installing anything?
